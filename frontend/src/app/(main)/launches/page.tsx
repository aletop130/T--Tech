'use client';

import { LaunchCorrelationPanel } from '@/components/Launch/LaunchCorrelationPanel';

export default function LaunchesPage() {
  return (
    <div className="h-full overflow-y-auto">
      <LaunchCorrelationPanel />
    </div>
  );
}
